[project]
name = "test-repo-pypi"
version = "1.0.0"
description = "Test fixture with known vulnerable Python dependencies"
requires-python = ">=3.8"
dependencies = [
    "requests==2.25.1",
    "flask==2.0.1",
    "pyyaml==5.4.1",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
]
